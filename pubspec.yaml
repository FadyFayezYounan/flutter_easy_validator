name: flutter_easy_validator
description: "package designed to simplify the process of validating strings in your applications."
version: 0.0.3
homepage: https://github.com/FadyFayezYounan/flutter_easy_validator.git

topics:
  - validation
  - validation-form
  - validators
  - form-validations
  - form-fields

environment:
  sdk: ">=3.2.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
